{
  "name": "process-calls",
  "version": "0.1.7",
  "description": "Make promisses and callbacks synchronous",
  "readmeFilename": "README.md",
  "main": "./dist/index.js",
  "scripts": {
    "build": "rimraf dist/ && babel ./ --out-dir dist/ --ignore ./node_modules,./.babelrc,./package.json,./README.md,./package-lock.json,./LICENSE,./npm-debug.log --copy-files",
    "start": "npm run build && node dist/index.js"
  },
  "keywords": [
    "promisse",
    "callback",
    "resolve",
    "async"
  ],
  "author": {
    "name": "erickstryck"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/erickstryck/process-calls"
  },
  "license": "MIT",
  "dependencies": {
    "babel-polyfill": "^6.26.0",
    "deasync": "^0.1.9"
  },
  "engines": {
    "node": ">=5.0.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "rimraf": "^2.6.2"
  }
}
